{
  "name": "bigyearpwa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "copy-data": "node scripts/copyDataToPublic.mjs",
    "verify-species-images": "node scripts/verifyLowercaseSpeciesImages.mjs",
    "dev:api": "node scripts/devBackend.mjs",
    "predev": "npm run copy-data && npm run verify-species-images",
    "dev": "vite",
    "prebuild": "npm run copy-data && npm run verify-species-images",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "idb": "^8.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.27.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "vite": "^5.4.11"
  }
}
