# Frontend environment variables (Vite)
# Copy to .env.local for local dev.

# Optional: enables backend persistence / sync.
# If unset, the PWA stays fully offline-only.
VITE_API_BASE_URL=http://localhost:8000
